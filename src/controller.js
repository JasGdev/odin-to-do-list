import listItem from './listItem.js'
import category from './category.js'

export default function controller() {
    const itemList = []
    // based on japan's tax on food items
    const tax = 0.08

    // tally of total spending
    const totalSpending = 0;
    const totalSpendingTax = totalSpending * (1 + tax);

    // add item
    let addItem = (name, cost, category, description, dueDate, paymentMethod, priority,) => {
        const newItem = listItem
        


    }

    // remove item

    // add additional information to a specific item

    // change information on items
}


